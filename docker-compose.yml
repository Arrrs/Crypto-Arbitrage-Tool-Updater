services:
  arb-updater:
    build: .
    container_name: arb-updater
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8082:8082"
    networks:
      - arbtool-network

networks:
  arbtool-network:
    external: true
